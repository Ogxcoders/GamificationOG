[package]
name = "gog-formulas"
description = "GamificationOG formula engine (Section 14) — deterministic, sandboxed expression evaluation"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gog-common = { path = "../common" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
