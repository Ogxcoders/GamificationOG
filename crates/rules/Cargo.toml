[package]
name = "gog-rules"
description = "GamificationOG rule engine (Section 13) — condition trees, 17 operators, deterministic evaluation"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gog-common = { path = "../common" }
serde = { workspace = true }
serde_json = { workspace = true }
regex = "1"
